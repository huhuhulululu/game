---
description: Claude Code Game Studios adapter for 并肩山谷 (Path D)
alwaysApply: true
---

This repo vendors Claude Code Game Studios under `.claude/`. Cursor-readable copies of the slash-command skills live in `.cursor/skills/` (same `SKILL.md` files).

Read `CLAUDE.md` first. Then `.claude/docs/technical-preferences.md`, `docs/adoption-plan.md`, `docs/WORLD.md`, `docs/CHARTER.md`, and `godot/docs/ART.md`.

- Project: 并肩山谷. Engine: Godot 4.4+ / GDScript. HTML5 first, iOS later.
- Brownfield Path D. Do not run `/brainstorm`. Do not scaffold a second Godot project.
- Do not overwrite `godot/`, `server/`, `src/`, `docs/WORLD.md`, or `docs/CHARTER.md`.
- `production/review-mode.txt` is `lean`. Do not stop for a director gate on every file.
- `production/stage.txt` is `Production`. Look is the open gap. TypeScript in `src/` is reference only.
- Next commands: `/setup-engine godot 4.4`, `/art-bible` from `godot/docs/ART.md`, `/asset-audit` on `godot/assets/art`.
- Unity / Unreal specialist agents are not in this tree. Use the Godot specialist set.
