# Control Manifest

> **Manifest Version**: 2026-08-15
> **Engine**: Godot 4.4.1
> **Review**: lean
> **Authority**: CHARTER / WORLD / ART. This file does not replace them.

## Foundation

- Required: Packed play/valley scenes. `Net` autoload. Cover read-only.
- Forbidden: Second Godot project. Unity. Authority sim in the client. Look in `src/`.
- Guardrail: HTML5 60 fps. Valley is one bed sheet.

## Core

- Required: `ValleyWorld.size_px`, `show_crops`. Actors `Look.BODY = 192`. Camera follow self.
- Forbidden: `prop-cover-*`, `prop-hut`, `prop-lodge` on the valley. Stall / anvil / pixel-ball trees on the valley floor.
- Guardrail: `Look.VALLEY_DUSK = Color(1,1,1)`. `dusk.gdshader` `grade` 0.0.

## Feature

- Required: Fish / mine / kitchen consume the same snap. Pair rules stay server-side.
- Forbidden: Overcooked rush outside the kitchen. Daily watering spine. 魂.
- Guardrail: Do not rewrite `src/sim/world.ts` for look.

## Presentation

- Required: Wood slip / plaque HUD. 做 / 喊. Mute does not send. Coat sheets have real a=0. Kitchen / mine / wild are one valley-language painting.
- Forbidden: Sanity rings, Wilson face, second beige form kit, orange full-screen grade. DOM title/subtitle under the canvas. Empty toast/order geom. Black / dusk plates behind coats. Empty-fill or collage place beds.
- Guardrail: HUD words, not hue-only bars.
